<h1> Hey this is the path of new articles</h1>

 
<%= form_for @article do |f| %>
<!-- Creating the Title Name (Label + Text Field ) -->
  <p>
     <%= f.label :title, "Title Name :-" %><br/>
     <%= f.text_field :title %>
  </p>
<!-- Creating the Desription Box (Lable + Text Area) -->
  <p>
    <%= f.label :description, "Description Box :-" %><br/>
    <%= f.text_area :description %>
  </p>
  <!-- Creating the new article button (Creat Article) -->
  <p>
     <%= f.submit  %>

  <%end%>